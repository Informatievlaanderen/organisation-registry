name: Manual Test NEW

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version'
        required: true

jobs:
  deployment:
    uses: ./.github/workflows/deploy-environment.yml
    with:
      environment: tst
      version: ${{ github.event.inputs.version }}
    secrets: inherit
