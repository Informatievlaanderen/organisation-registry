import { NgModule } from '@angular/core';
import { RouterModule, Routes } from '@angular/router';

import { PurposeService } from './purpose.service';

@NgModule({
  declarations: [
  ],
  providers: [
    PurposeService
  ],
  exports: [
  ]
})
export class PurposesServiceModule { }
