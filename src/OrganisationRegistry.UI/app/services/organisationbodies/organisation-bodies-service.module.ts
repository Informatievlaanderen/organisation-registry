import { NgModule } from '@angular/core';
import { RouterModule, Routes } from '@angular/router';

import { OrganisationBodyService } from './organisation-body.service';

@NgModule({
  declarations: [
  ],
  providers: [
    OrganisationBodyService
  ],
  exports: [
  ]
})
export class OrganisationBodiesServiceModule { }
