import { NgModule } from '@angular/core';
import { RouterModule, Routes } from '@angular/router';

import { RegulationSubThemeService } from './regulation-sub-theme.service';

@NgModule({
  declarations: [
  ],
  providers: [
    RegulationSubThemeService
  ],
  exports: [
  ]
})
export class RegulationSubThemesServiceModule { }
