import { NgModule } from '@angular/core';
import { RouterModule, Routes } from '@angular/router';

import { ProjectionService } from './projection.service';

@NgModule({
  declarations: [
  ],
  providers: [
    ProjectionService
  ],
  exports: [
  ]
})
export class ProjectionsServiceModule { }
