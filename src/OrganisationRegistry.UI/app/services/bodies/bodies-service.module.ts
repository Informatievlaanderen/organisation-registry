import { NgModule } from '@angular/core';
import { RouterModule, Routes } from '@angular/router';

import { BodyService } from './body.service';

@NgModule({
  declarations: [
  ],
  providers: [
    BodyService
  ],
  exports: [
  ]
})
export class BodiesServiceModule { }
