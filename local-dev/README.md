# Local Development

Deze map bevat lokale ontwikkel-infra.

- `k8s`: Kubernetes manifests voor de Tilt/k3d setup.
- `k8s/manual`: bewaarde manifests die niet automatisch door Tilt geladen worden.
- `helm`: Helm values voor lokale dependencies zoals Traefik.

Demo-applicaties staan onder `demo`.
